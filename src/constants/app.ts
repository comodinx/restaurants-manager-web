/**
 * Variable used for logs, health check, etc
 */
export const name = process.env.APP_NAME || "application";

/**
 * Variable used for logs, health check, etc
 */
export const version = process.env.APP_VERSION || "0.0.1";
